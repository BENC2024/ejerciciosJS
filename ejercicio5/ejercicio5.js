let lista = [1,5,3,4,7,9]; //Haciendo suma de datos de la lista
let resultado = 0;

for (let index = 0; index < lista.length; index++) {
   resultado = resultado + lista[index];
}

console.log(lista);
console.log(resultado);